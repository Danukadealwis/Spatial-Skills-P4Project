namespace DefaultNamespace
{
    enum AnswerButtonState
    {
        Active = 1,
        Disabled = 2,
        Reset = 3
    }

    enum TimerState
    {
        QuizStart = 1,
        TimeElapsed = 2,
        QuizComplete = 3
        
    }
}